node_modules
packages/*/dist
packages/*/.svelte-kit
packages/*/*.tsbuildinfo
*.tsbuildinfo

# Convex — CLI-managed local state; convex/_generated IS committed (plain
# codegen from schema/functions, no secrets), only this local cache isn't.
.convex

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/Certs
/Logo and Apple directions
/reference
/admin-panel-v2
coverage

# OS
.DS_Store
Thumbs.db
# macOS AppleDouble resource-fork shadow files — this filesystem produces
# one per real file (._foo instead of foo); never commit these.
._*

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

CLAUDE_CODE_BUILD_SPEC_V2_SUPABASE.md

# Testing guides contain real dev-deployment credentials (admin password,
# API keys) — never commit these. A prior version was committed as a PDF
# with the admin password in plaintext; see docs/AUDIT.md's 2026-09-17
# critical finding.
Norrone-Testing-Guide.pdf
*-Testing-Guide.pdf